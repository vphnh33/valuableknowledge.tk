# baseurl = "https://www.valuableknowledge.tk"
baseurl = "/"
relativeURLs = true
title = "Mysterious Unicorn's Blog"
languageCode = "en-us"
paginate = "10" # Number of posts per page
theme = "hugo-mainroad-theme"
# disqusShortname = "" # Enable comments by entering your Disqus shortname
# googleAnalytics = "" # Enable Google Analytics by entering your tracking id

[Author]
  name = "Mysterious Unicorn's Blog"
  bio = "The mysterious unicorn's true identity is unknown. Maybe she is a successful blogger or writer. Nobody knows it."
  avatar = "img/unicorn-3637428.png"

[Params]
  subtitle = "Valuable Knowledge!" # Subtitle of your site
  description = "Mysterious Unicorn's Personal Blog About Valuable Knowledge" # Description of your site
  opengraph = true
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  post_navigation = true
  post_meta = ["date", "categories"] # Order of post meta information

[Params.sidebar]
  home = "right"   # Configure layout for home page
  list = "right"   # Configure layout for list pages
  single = "right" # Configure layout for single pages
  # Enable widgets in given order
  widgets = ["search", "recent", "categories", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)